{
  "synchronousCommands":{
    "description":"Each command executed to completion before the next is started in array order",
    "commands":[
      {
        "description":"List files",
        "command":"ls",
        "args":["F","a","l"],
        "sudo":{}
      }
    ]
  }
}
