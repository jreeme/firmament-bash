{
  "description":"test description valid",
  "prerequisiteGraph": "https://raw.githubusercontent.com/jreeme/firmament-bash/master/command-json/valid-01.json",
  "asynchronousCommands": {
    "commands": [
      {
        "description": "List files",
        "command": "ls",
        "args": [
          "-F",
          "-a",
          "-l"
        ]
      }
    ]
  },
  "serialSynchronizedCommands": {
    "commands": [
      {
        "description": "Take a look at network",
        "command": "ifconfig",
        "args": [
          "-a"
        ]
      }
    ]
  }
}
